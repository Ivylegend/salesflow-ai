import {cn,initials,relativeDate} from "@/lib/utils";
import Link from "next/link";
import type {Lead} from "@/lib/api";
export function PageHeader({eyebrow,title,description,action}:{eyebrow?:string,title:string,description?:string,action?:React.ReactNode}){return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>{eyebrow&&<div className="eyebrow mb-2">{eyebrow}</div>}<h1 className="text-2xl font-bold tracking-[-.035em]">{title}</h1>{description&&<p className="mt-1 text-sm text-[#717a76]">{description}</p>}</div>{action}</div>}
export function Score({score,classification}:{score:number|null,classification:string|null}){return score===null?<span className="text-xs text-[#8a928e]">Processing</span>:<span className={cn('pill',classification?.toLowerCase())}><span>{score}</span><span className="font-medium">{classification}</span></span>}
export function LeadRow({lead}:{lead:Lead}){return <Link href={`/dashboard/leads/${lead.id}`} className="grid min-w-[760px] grid-cols-[1.4fr_1.2fr_.7fr_1fr_.7fr] items-center gap-4 border-b border-[#eceeec] px-4 py-3 text-sm hover:bg-[#fafbfa]"><div className="flex items-center gap-3"><div className="grid h-8 w-8 place-items-center rounded-full bg-[#eef2ef] text-[10px] font-bold text-[#52615b]">{initials(lead.full_name)}</div><div><div className="font-bold">{lead.full_name}{lead.is_demo&&<span className="ml-2 rounded bg-[#eee9df] px-1.5 py-0.5 text-[9px] uppercase tracking-wider text-[#786c55]">Demo</span>}</div><div className="text-xs text-[#7a837f]">{lead.email}</div></div></div><div><div className="font-medium">{lead.company}</div><div className="text-xs text-[#7a837f]">{lead.job_title}</div></div><Score score={lead.score} classification={lead.classification}/><span className="text-xs font-medium">{lead.stage}</span><span className="text-xs text-[#7a837f]">{relativeDate(lead.created_at)}</span></Link>}
export function Empty({children}:{children:string}){return <div className="panel p-10 text-center text-sm text-[#737c78]">{children}</div>}

