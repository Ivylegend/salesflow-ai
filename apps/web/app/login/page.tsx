"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
import Link from "next/link";
import {API_URL} from "@/lib/api";
import {Logo} from "@/components/marketing";
export default function Login(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const body=new URLSearchParams({username:String(f.get('email')),password:String(f.get('password'))});const r=await fetch(`${API_URL}/api/auth/login`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});if(r.ok){localStorage.setItem('sf_token',(await r.json()).access_token);router.push('/dashboard')}else{setError('Incorrect email or password.');setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-[#f2f4f1] p-5"><div className="w-full max-w-sm"><div className="mb-8 flex justify-center"><Logo/></div><form onSubmit={login} className="panel p-7 shadow-sm"><h1 className="display text-3xl">Welcome back.</h1><p className="mt-2 text-sm text-[#6d7672]">Sign in to your revenue workspace.</p><div className="mt-7"><label className="label" htmlFor="email">Email</label><input required type="email" name="email" id="email" className="field" autoComplete="email"/></div><div className="mt-4"><label className="label" htmlFor="password">Password</label><input required type="password" name="password" id="password" className="field" autoComplete="current-password"/></div>{error&&<p className="mt-4 text-sm text-red-700">{error}</p>}<button className="button button-primary mt-6 w-full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><Link href="/" className="mt-5 block text-center text-xs text-[#68716d]">← Return to website</Link></div></main>}

